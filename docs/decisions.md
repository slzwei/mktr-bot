# Implementation decisions

- C2a: Share CSV parsing and deduplication between free preview and import, then require the displayed `maxDncCredits` on enabled imports, because changing consent or expiry must never increase spending beyond the operator's confirmation.
- C2a: Add checking counts under an optional `dnc` import result and expose separate authenticated preview, summary and single-check routes because the disabled import contract must remain byte-for-byte unchanged while permission reads stay free.
- C2a: Cancel the draft's automatic paid expiry sweep and expose expiry warnings plus an explicit one-credit action because every paid check requires a visible cost and an operator click; valid evidence, including near-expiry results, is never rechecked.
- C2a: Serialize checks per API/store, recheck coverage before each batch and omit fresh negative results as well as consent and fresh clearances because simultaneous actions and already-known registration must not consume duplicate credits.
- C2a: Require HTTP 200 and a complete, exact S000 result set before any batch writes, preserve earlier successful batches after a later failure, and return failed/unsent counts because partial or ambiguous evidence must never become voice permission.
- C2a: Bound each gateway request to ten seconds and each response to 128 KB, refuse redirects and never retry automatically because a lost response may already have spent prepaid credits; report submitted numbers and billing uncertainty instead of inventing a balance or exact failed-request charge.
- C2a: Store the Registry creation time, validity date and three register flags in the existing append-only JSON snapshot because that preserves provider evidence without a migration while leaving the receipt-based 21-day dial gate untouched.
- C2a: Keep registered contacts stored and selectable, and derive the aggregate UI permission summary from `ConsentPolicy`, because campaign selection does not replace the enforcement gate and later valid consent may change permission.
- C2a: Accept only HTTPS gateway URLs outside loopback, require a dedicated secret of at least 32 characters and pass configuration through Compose with the flag defaulting off because tests need fake local HTTP while deployed requests need authenticated transport.

- **B3 — one classification deadline and rules fallback:** use the configured model with a 1500 ms default `AbortSignal.timeout`, zero SDK retries, output validation, and a rules fallback counter because retrying a slow classifier extends audible silence while deterministic fallback can immediately route the call.
- **B3 — refusal before acceptance:** evaluate explicit do-not-call/product refusal before callback and positive words, but allow bare “no” before a request to call later, because Singapore English acceptance and temporary-unavailability phrases need distinct routing without negation reversals; 89 fixtures preserve these decisions.

- **B2 — readiness through the event connection:** query Singtel gateway status through the existing ESL client, accept only connected `REGED`, and bound/cache/coalesce probes because a healthy HTTP process does not prove trunk readiness and health checks must not congest call commands.
- **B2 — local aggregate metrics:** use pino with request/call correlation and `prom-client` with fixed outcome/provider labels, expose metrics only through the local API/private network, and keep counters process-local because a single-host deployment needs inspectable monitoring without external services or high-cardinality personal data.

- A2: Use one serialized, reconnecting ESL connection and never replay an interrupted command because repeating an originate can create a second billable channel. The protocol follows the [FreeSWITCH event socket reference](https://developer.signalwire.com/freeswitch/integration/event-socket/).
- A2: Reserve and store the provider UUID before origination, match events to that UUID, and tag every playback because provider events can precede command replies and duplicate playback notifications must not advance a later node.
- A2: Terminate the provider channel before releasing its logical slot; if hangup cannot be confirmed, retain the slot and expose the error so a reconnect or operator retry can reconcile it.
- Verification: Keep simulator processes and fake ESL/STT tests separate from operator trunk checks; Docker-dependent checks cannot run on this host until the operator installs Docker.
- A7: Use argon2id (64 MiB, three iterations, one lane) with environment-seeded email/password accounts because MKTR operates a single host and needs no external identity provider. Seeding never overwrites an existing account.
- A7: Store only SHA-256 hashes of random 256-bit session tokens, with an eight-hour expiry, rotation on sign-in and revocation on logout; this limits exposure if session rows are disclosed. Cookies always use `Secure`, `HttpOnly`, `SameSite=Strict` and the `__Host-` prefix.
- A7: Require authentication for every control-plane read and write, apart from health and separately bearer-authenticated media webhooks, because transcripts and operational state are also private. Use per-operator call limits and per-IP sign-in limits in this single-process deployment.
- A7: Keep telephony mode in the administrator's deployment environment and require a restart because switching adapters while calls are active can orphan channels. The authenticated admin endpoint reports this requirement and never enables live calls.
- A7: Use Caddy for automatic HTTPS and explicit immediate SSE flushing; a separate private proxy network allows Express to trust only Caddy's fixed address. See [Caddy reverse proxy documentation](https://caddyserver.com/docs/caddyfile/directives/reverse_proxy) and [Express proxy trust guidance](https://expressjs.com/en/guide/behind-proxies/).
- A7: Use pino JSON logs with request IDs and secret-field redaction; return generic unexpected errors so operators can correlate failures without exposing internal messages to browsers.

- **A4 — source-built FreeSWITCH:** compile pinned FreeSWITCH v1.11.3, sofia-sip, spandsp, and `mod_audio_stream` from their public repositories because the existing image tag was unverified and a build makes the streaming module and security release explicit; source revisions were checked, but Docker build and runtime registration are operator verification gates.
- **A4 — private bridge and explicit advertised IP:** use a static telephony bridge with API `.2`, worker `.3`, and gateway `.4` on `172.29.80.0/24`, while rendering the distinct `MKTR_GATEWAY_PUBLIC_IP` into SIP/RTP advertisements, so ESL can later be restricted to known private peers and the trunk receives a routable media address.
- **A4 — local RTP slice:** expose local UDP 10000–10199 in both FreeSWITCH and compose because 200 RTP/RTCP ports comfortably cover the five-call ceiling; Singtel's remote UDP 10000–30000 range remains unchanged.
- **A4 — startup rendering and TLS identity:** render the complete overlay at entry from environment and secret files into private runtime files, install the Singtel bundle as `cafile.pem`, and require a matching gateway certificate/private key as `agent.pem`, because TLS needs both peer trust and a listener identity and neither belongs in Git.
- **A4 — authenticated media WebSocket:** use `mod_audio_stream` with `STREAM_EXTRA_HEADERS` carrying the bearer header and mono 8 kHz receive audio, because the pinned upstream module supports this handshake and emits little-endian 16-bit PCM on the selected Linux platforms; see `docs/freeswitch-deployment.md` for source links and application disconnect responsibilities.
- **A5 — explicit ESL peers and startup guard:** bind ESL only to the gateway's private interface, permit only the API/worker `/32` addresses, omit the host port mapping, and reject the default/short/framed password in a shared pure startup guard, because private networking and authentication should each prevent an unintended caller from controlling the trunk; operator diagnostics share the API network namespace instead of weakening the ACL.
# Streaming media (A1)

Deepgram Nova-3 is the first `SpeechToText` implementation because its WebSocket API supports final results and configurable 750 ms endpointing with linear16/8 kHz mono audio. `MKTR_STT_PROVIDER` selects it explicitly; unsupported providers fail at startup. `MKTR_STT_LANGUAGE` defaults to the application locale `en-SG`, mapped to wire code `en` because [Deepgram's supported-language table](https://developers.deepgram.com/docs/models-languages-overview) does not list `en-SG`. Singapore pronunciation accuracy remains an operator check; no real provider request was made.

FreeSWITCH starts `mod_audio_stream` only after a UUID listen window is persisted and closes it before classification. The worker authenticates both audio upgrades and API requests, checks the current window, buffers at most two seconds while connecting, and uses one stable utterance receipt across HTTP retries. One completed utterance closes the window, preventing duplicate or late transcripts from advancing another turn. Worker and API share the root lockfile and compiler, with separate Dockerfiles/processes, to avoid dependency drift. Simulator workers expose health with streaming disabled and need no provider key.

The A1 robustness review separates active audio from pending receipt delivery because the API may close media or start its next listen before its previous HTTP response arrives. Five active windows remain the ceiling; a separate bound of 25 total reservations prevents stalled receipts from accumulating indefinitely. Opening and API operations each have a three-second deadline, and cancellation closes late provider streams without replaying their buffered audio.

STT latency uses the final recognized word's timestamp mapped to the first PCM arrival because silence continues producing audio frames after speech ends. Missing or inconsistent provider timing is omitted; the resulting estimate includes endpointing and transport and is not precise provider compute latency, consistent with [Deepgram's latency guidance](https://developers.deepgram.com/docs/measuring-streaming-latency).

## Time limits (A3)

Listen windows default to 6000 ms and use the explicit fallback on silence. Retry nodes default to one attempt, require an explicit counter when they can reach a listen, and end the call on exhaustion unless an exhaustion fallback is supplied. Both provider-owned originate/answer timers and API deadlines enforce 30-second answer and 180-second answered-call limits, so ESL/API disruption cannot leave an indefinitely parked call. Invalid capacity (outside 1–5) and timeout environment values fail startup instead of weakening the guards.

## Restart and shutdown (A6)

A restart or ESL reconnection terminates interrupted MKTR calls instead of guessing which media event was missed. The API inventories channels, checks the exact `origination_caller_id_name` variable, kills unknown MKTR UUIDs, preserves unrelated channels, and records interrupted stored calls as failed. New dials remain disabled if inventory or cleanup fails. SIGTERM/SIGINT stop listeners and SSE, send one idempotent hangup per active call, close ESL, and use a 15-second process deadline within Compose's 20-second grace period. FreeSWITCH's independently scheduled duration limit is the last bound if ESL cannot confirm shutdown. The API runs Node directly as PID 1.

Channel inventory and variable inspection use the [FreeSWITCH API command interface](https://developer.signalwire.com/freeswitch/reference/cli-and-api/); behavior here is verified against fake ESL only.
- B1: Use Prisma 6.19.3 with the existing PostgreSQL schema style because it supports the pinned Node runtime without requiring a driver adapter migration; pin the matching CLI/client together. [Prisma system requirements](https://www.prisma.io/docs/orm/reference/system-requirements) and [transaction guidance](https://www.prisma.io/docs/orm/v6/prisma-client/queries/transactions) inform the implementation.
- B1: Override Prisma's transitive `deepmerge-ts` dependency to 8.0.2 to remove its audited recursive-merge vulnerability; the relevant Prisma usage merges plain configuration objects and the actual generate, migration and database suite pass with the override. [Upstream release notes](https://github.com/RebeccaStevens/deepmerge-ts/releases/tag/v8.0.2) document the package change.
- B1: Keep a single-process synchronous read cache with ordered transactional writes and an explicit `flush` barrier because the event-driven orchestrator must match provider events immediately; latch any write failure until restart so an uncommitted UUID cannot authorize a new provider action.
- B1: Store every published graph immutably, enforce that rule with a database trigger, and retain full call JSON snapshots alongside indexed columns because later media/outcome fields must survive restart without changing the historical graph.
- B1: Quarantine unclaimed clip files and archive records with missing bytes because recovery should preserve uploaded content and published references; deletions retain immutable flow history.
- B1: Default runtime state to Prisma and require an explicit memory-store setting for disposable simulator development because a missing database must never silently reset production state.
- B1: Run the same database behavior suite against a disposable native PostgreSQL 17 cluster on this host because Docker is absent; the Compose PostgreSQL 16 path was initially blocked and subsequently passed on GitHub's Docker runner without installing Docker locally.

- A8: Keep the first destination and review explicitly pending Shawn's designation because a fixture phone number is not authorization to call a real recipient; the runbook fixes the first caller ID and reduces capacity to one with a 60-second answered-call limit.
- A8: Validate every runbook command with shell parsing, then test the renderer/config inspector and private diagnostics through isolated fixtures because deployment syntax checks cannot establish real TLS, provider, Docker or trunk behavior.
- A8: Run diagnostics in an existing allowed API/worker network namespace with a read-only private fs_cli profile and `-Q`, because the existing /32 ESL ACL should not be widened to support troubleshooting; the pinned fs_cli source uses `no-history-file`, so the deployment note was corrected.
- A8: Inspect resolved Compose JSON without printing its secret fields and reject incorrect first-call limits, mode disagreement or published ESL because env-file edits and shell precedence can otherwise produce a deployment different from the operator's intent.
- B4: Retain the original bytes at `assetUrl`/`previewUrl` and add `telephonyAssetUrl` for a distinct 8 kHz mono signed-16-bit WAV because operators need faithful previews while FreeSWITCH needs a predictable codec; duration is measured from decoded audio and rounded to the existing whole-second database field.
- B4: Force the WAV/MP3 demuxer after signature checks, restrict FFmpeg protocols to local file/pipe, bound subprocess time/output and allow two conversions at once because uploaded bytes should not initiate network fetches or consume unbounded process resources. [FFmpeg protocol controls](https://ffmpeg.org/ffmpeg-protocols.html) and [ffprobe output documentation](https://ffmpeg.org/ffprobe.html) define the selected options.
- B4: Archive clips referenced by any immutable published version, even when its current flow is deleted, and flush metadata before removing unreferenced files because historical/active flow media must remain available and database failures must not delete required audio.
- B4: Validate request shapes separately from publish-time graph reachability because operators need to save incomplete drafts while malformed node types, coordinates, identifiers and counter values must never enter storage.
- B6: Pin Node 24.14.0 across the local version file, package engine, image and CI because it matches the verified host runtime and avoids deployment/runtime drift; the final image runs direct Node as UID 1000.
- B6: Load the names-only development environment with Node's `--env-file`, treating empty optional settings as unset while requiring explicit admin/database inputs, because shell sourcing is unnecessary and blank paths must not break the documented simulator startup.
- B6: Upload only Playwright failure screenshots to CI artifacts because authentication state and browser traces can contain session cookies even when they belong to disposable test accounts.
- B6: Exclude generated `runtime/`, credentials, key/certificate formats, nested env/session files and Git/worktree data from image build contexts so a production-host build cannot accidentally bake mounted secrets into layers; certificate README placeholders remain available.
- B6: Keep Postgres credentials in environment with no fallback password, and preserve Prisma CLI/migrations in the runtime image because migrations must run before the application starts.
- B6: Use custom-format `pg_dump` plus a full clip-volume archive with checksums and an API maintenance window because the database and media files need a consistent recovery point. Restore to empty targets, and verify a disposable real PostgreSQL drill without touching the operator's service. See [PostgreSQL backup format documentation](https://www.postgresql.org/docs/17/app-pgdump.html).
- B5: Send a complete call snapshot with its latest event ID and refetch on every stream open because durable state is already available through the authenticated call API; this avoids a second replay store while repairing missed events across process restarts. Native EventSource retries remain enabled, with a one-second replacement only for a permanently closed transport. See the [browser SSE reference](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events).
- B5: Refresh calls and trunk capacity every two seconds and on focus because there is one API process and at most five active calls; keeping flow and clip state outside that refresh preserves unsaved operator edits.
- B5: Use 15-second comment heartbeats, `no-transform`, `X-Accel-Buffering: no` and Caddy's `flush_interval -1` because intermediate buffering and idle connection expiry must not stall the event timeline. A disconnected or slow consumer releases its server subscription.
- B5: Forward abrupt SSE upstream closure through Vite's development proxy because the actual API-crash test exposed an open downstream response after the API socket died; without this forwarding the browser never receives the disconnect needed to reconnect.
- B5: Verify restart recovery with a disposable native Postgres cluster and a real killed simulator API because browser request interception alone cannot prove durable sessions and call snapshots survive a process restart; both browser sessions and the database belong only to the test.
- C1: Use one paced scheduler in the supported single API process, with queued ticks/controls and the orchestrator's shared concurrency guard, because a second worker or Redis queue would add coordination without helping a five-channel trunk.
- C1: Pin immutable flow versions, approved caller IDs and contact membership when creating a campaign because later editor/import changes must not change the audience or script of a running campaign.
- C1: Normalize Singapore local numbers and explicit international prefixes, validate the entire CSV before one transaction, and deduplicate by E.164 phone because partial imports and duplicate phone rows cause unintended calls.
- C1: Default to Monday–Saturday 09:00–20:00 Singapore time, three attempts and a 60-second busy/no-answer delay; support one same-day interval and configurable limits because these choices satisfy the calling-hours/retry contract with predictable operator controls.
- C1: Persist an attempt before originate and mark an interrupted unconfirmed attempt for review because automatically replaying an ambiguous provider side effect could call a contact twice; shutdown awaits the scheduler before draining calls.
- C1: Reuse the every-dial consent gate and surface policy skip reasons without consuming attempts because importing contacts must never imply permission to call.

- C2: Require recorded voice-marketing consent or an affirmative manual Singapore No Voice Call Registry result at every originate, including simulator calls, because a CSV import or prior campaign is not evidence of permission; opt-outs override clearance and only later consent can restore permission.
- C2: Expire DNC clearance at 21 days, stricter than the checklist's 30-day maximum, because the [PDPC business rules](https://www.pdpc.gov.sg/overview-of-pdpa/do-not-call-registry/business-owner/do-not-call-registry-business-rules) specify a 21-day validity period; the operator obtains and records actual Registry evidence without the application registering an account.
- C2: Store consent, withdrawal and DNC results as immutable append-only evidence with server recording timestamps, and recheck after the call snapshot is durable because concurrent withdrawals must block provider side effects. Tests for unrelated features explicitly inject a fake policy from excluded test-support code.
- C2: Ask operators to enter actual evidence timestamps explicitly in Singapore time because browser locale or an automatic current timestamp cannot establish when consent or a Registry check occurred.
- C2/A6: Recheck authorization, evidence identity and the shutdown latch immediately before an ESL socket write because queued commands may outlive their initial permission check; shutdown stops new dials before awaiting campaign drainage and attempts all cleanup steps even after a failure.
- C3: Use a Postgres outcome outbox with one immutable delivery ID/body per call and persist each attempt before HTTP because receiver acknowledgement can be lost across an API crash; recipients deduplicate the stable ID.
- C3: Limit webhook attempts to eight, including a reserved final attempt after a crash, with bounded exponential delays, three-second requests and no redirects because notification failures must not block calls or retry indefinitely.
- C3: Require exact administrator-configured HTTPS hosts and a private HMAC secret, and restrict per-campaign endpoint edits to administrators because outcome payloads contain contact and call data.
- C3: Apply webhook changes to future completions and preserve queued delivery endpoints because enabling a setting must not silently export historical calls or redirect an existing event to another recipient.
- C3: Quote CSV fields, collapse embedded newlines and prefix spreadsheet formula cells with an apostrophe because the export must preserve one physical row per call without turning imported text into executable formulas.

- C3: Start FreeSWITCH's built-in `mod_avmd` on answer and treat a detected callee beep as voicemail because it supplies a bounded local signal without another external provider; it is a beep detector, and machine/false-positive accuracy remains an operator check.
- C3: Keep recordings opt-in, store only generated filenames in a dedicated shared volume, and purge on boot/hourly with a 30-day default because call audio needs an explicit retention lifecycle separate from the reusable clip library.
- C3: Preserve a requested terminal reason when a provider hangup races the kill reply because normal channel clearing must not erase voicemail, flow-failure or operator-stop outcomes.
- C3: Persist termination intent and retain it through unconfirmed hangup/reconnect, and reject stale active snapshots after terminal completion, because async media shutdown or delayed ESL replies must neither resurrect a call nor erase its outcome. Provider hangup is still attempted if recording that intent fails.

- C4: Start new drafts with a two-node Start → End graph because a blank workflow should not silently inherit the demo's clips or routing decisions.
- C4: Reuse the durable call event history for the latest 200 operator log entries because it ties timestamps and outcomes to one call without exposing unrelated server or credential logs.
- C4: Allowlist read-only telephony limits and classifier settings, and serve the fixed runbook path behind the existing session, because operators need current deployment guidance without disclosure of provider credentials or an in-browser live-mode switch.
- C4: Reuse flow soft deletion and published-reference clip archival for the UI controls because immutable history and its audio must survive routine editor cleanup.
- C5: Keep callbacks disabled until a ready uploaded greeting and Singtel routing are confirmed; generate the DID expression from `CALLER_IDS` because the same authoritative pool must govern inbound and outbound identities.
- C5: Apply an explicit carrier source `/32` ACL with no digest fallback and render FreeSWITCH `max-sessions` from the shared 1–5 ceiling because incoming legs must not bypass the trunk limit or admit public SIP scanners. The supplied account IP is a configurable starting value, not a claim that the inbound delivery source is confirmed.
- C5: Let the FreeSWITCH dialplan own greeting/optional message capture while a separate raw-ESL tracker persists the callback, because incoming calls must not execute the outbound orchestrator graph. Pin the greeting in an immutable internal flow version so clip deletion preserves its bytes.
- C5: Keep recording disabled by default, use bounded input recording and channel-UUID filenames, and attach retention metadata only after positive recorded duration because the greeting/notice and retention decision belong to the operator while paths and durations need enforceable limits.
- C5: Retain the pinned AVMD source defaults for stable frequency-plus-amplitude detection on the outbound read stream because C3 consumes the callee's beep event; the build checks that `mod_avmd.so` exists, and source/config verification does not claim live detection accuracy. Primary references and outstanding carrier checks are in `docs/inbound-callbacks.md`.
- B6: Keep `.env.example` as a names-only inventory and document explicit private env-file loading because the Node API does not load `.env` automatically and launch instructions must supply authentication settings while preserving simulator defaults.
- B6/A8: Separate pre-call infrastructure gates from the first controlled media test, and keep callbacks, recordings, campaigns and webhooks off during that window, because one approved destination must not silently authorize additional traffic or data delivery.
- A4: Build the complete pinned gateway image in a separate, path-filtered GitHub job with a 45-minute limit because the local host has no Docker and source assembly needs direct verification; keeping the job free of runtime startup and credentials preserves the operator's live-call gate, while documentation-only evidence commits avoid unnecessary rebuilds.
- B6/A1/A6: Verify built API and worker images on an internal, disposable Docker network in GitHub Actions because production startup, healthchecks, non-root PID 1 and actual Docker stop behavior can be checked without installing Docker locally or enabling a real gateway.
- A4: Install Debian's `libpcre2-dev` in the builder and `libpcre2-8-0` in the runtime because the pinned FreeSWITCH configure script requires PCRE2; the first GitHub gateway build exposed the obsolete PCRE1 dependency before any gateway process could start.
- B6: Compare PID 1’s executable with Node’s executable during image verification because Node 24 reports a Linux thread name of `MainThread`; a thread label does not establish which executable owns PID 1.
- A4: Include `libevent-pthreads-2.1-7` in the gateway runtime because `mod_audio_stream` links libevent's threading support, and print shared-library check failures because successful compilation alone must not hide an incomplete runtime image.

- B6: Run container HTTP probes inside the disposable internal network because Docker intentionally suppresses host publication on that network; this preserves network isolation while testing the images through their real HTTP interfaces.
- A4: Rely on the pinned FreeSWITCH core's built-in PCMA/G.711 registration because that core is compiled and preloaded before the overlay module list; a separate codec module is unnecessary, while actual negotiation still requires the operator's controlled call.
